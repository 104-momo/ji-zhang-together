# 一起记账 · 云端部署指引
> 把本地单人版升级为**两人实时共享版**，数据存在你自己的腾讯云账号下。
> 全程免费额度内可跑，预估 0~10 元/月，约 15 分钟完成。
---
## 第一步：注册腾讯云并开通云开发
1. 打开 <https://cloud.tencent.com/>，注册账号（微信扫码即可）
2. 实名认证（个人认证，免费，需要身份证号）
3. 打开云开发控制台：<https://console.cloud.tencent.com/tcb>
4. 点「新建环境」，环境名称随便填（如 `ji-zhang`），计费方式选「按量计费」（有免费额度，小圈子基本不花钱）
5. 等待环境创建完成（约 1~2 分钟）
6. 进入环境，在「环境 → 环境 ID」处复制环境 ID（格式类似 `ji-zhang-3gxxxxxx`），发给我
---
## 第二步：开启邮箱登录（用户注册登录的关键）
1. 云开发控制台 → 左侧「身份验证」
2. 找到「邮箱登录」，点「启用」
3. 在邮箱登录配置里，**关闭「邮箱验证」**（这样用户注册后不用去邮箱点验证链接，直接登录；如果需要验证可自行开启）
4. SMTP 配置：可以用腾讯云的默认邮件服务，或者自己配 QQ/163 邮箱 SMTP（注册验证邮件用，关闭验证后可不配）
5. 状态变为「已启用」即可
> 为什么用邮箱登录：数据和用户账号绑定，换设备登录后数据还在；未来上线 App Store 时，在邮箱登录基础上加 Apple 登录即可满足审核要求，不用大改架构。
---
## 第三步：创建数据库集合并配置安全规则
在云开发控制台 → 左侧「数据库」，依次创建以下 3 个集合（点「+ 新建集合」，名字必须完全一致）：
| 集合名 | 用途 |
|---|---|
| `ledgers` | 账本（名称、创建者、邀请码、自定义分类） |
| `members` | 成员（昵称、加入时间） |
| `entries` | 账目（金额、分类、备注、修改记录） |
**每个集合创建后，配置安全规则**（点集合 → 「安全规则」标签 → 编辑 → 粘贴下面的内容 → 保存）：
```json
{
  "read": "auth != null",
  "write": false
}
```
> 含义：只有**登录用户**可以读（用于实时推送），未登录的人看不到任何数据；**所有写操作必须走云函数**（云函数用管理员权限，且内部做权限校验）。三个集合都配成一样的规则。
---
## 第四步（我来做）：部署云函数 + 前端
你把**环境 ID**发给我之后，我会：
1. 用 CloudBase CLI 部署 `ledgerApi` 云函数到你的环境
2. （可选）如果你提供了智谱 API Key，配置到云函数环境变量，开启 LLM 兜底解析
3. 构建前端（注入环境 ID）并部署到 CloudBase 静态托管
4. 给你一个稳定的访问网址（类似 `https://ji-zhang-xxx.tcloudbaseapp.com`）
5. 你和对象各自用手机打开网址 → 填昵称 → 一人创建账本、分享邀请链接 → 另一人点开加入 → 开始记账
---
## 可选：智谱 GLM API Key（提升复杂句子识别率）
不配置也能用——规则解析能覆盖"吃烤鱼200""打车35.5"等 90% 常见句式。配置后，规则解析不了的复杂句子会自动调 AI 兜底分类。
1. 打开 <https://open.bigmodel.cn/>，注册登录
2. 左侧「API Keys」→ 「创建新的 API Key」→ 复制
3. 把 Key 发给我，我配置到云函数环境变量 `ZHIPU_API_KEY`
> 智谱 GLM-4-Flash 模型免费，不花钱。
---
## 验证清单（部署完成后）
- [ ] 手机 A 打开网址 → 注册账号（邮箱+密码+昵称）→ 自动登录
- [ ] 手机 A 创建账本 → 复制邀请链接
- [ ] 手机 B 打开网址 → 注册另一个账号 → 打开邀请链接 → 自动加入同一本账
- [ ] 手机 A 记一笔"午餐30元"，手机 B 上立刻冒出来（实时推送）
- [ ] 手机 B 记一笔"打车20元"，手机 A 上立刻冒出来
- [ ] 两人都能看到本月总支出、分类统计、按人筛选
- [ ] 每人只能改/删自己的账；创建者能改/删任何人的账
- [ ] 关掉网页重新打开，自动保持登录态，数据还在（云端持久化）
- [ ] 换一台手机登录同一账号，能看到历史账目
- [ ] 首页点「退出登录」后，需要重新登录才能看到数据
---
## 常见问题
**Q：数据安全吗？别人能看到我的账吗？**
A：账本不公开，只有拿到邀请链接的人能加入。所有写操作在云函数里校验权限，前端无法越权修改。数据存在你自己的腾讯云账号下，你随时可以导出或删除。
**Q：会扣费吗？**
A：CloudBase 有免费额度（数据库 2GB、云函数每月 4 万次调用、静态托管 5GB 流量）。两个人日常记账完全在免费额度内，预估 0~10 元/月（超出按量计费也很便宜）。
**Q：邀请链接泄露了怎么办？**
A：账本创建者可以在「管理」里点「重新生成邀请码」，旧链接立即失效，再把新链接发给对方即可。
**Q：能导出数据吗？**
A：可以在云开发控制台 → 数据库 → 对应集合 → 导出，选 JSON 或 CSV 格式下载。
